# Cloudflare Account ID
CF_ACCOUNT_ID=你的 Cloudflare 帳號 ID

# Cloudflare API Token
CF_API_TOKEN=你的 Cloudflare API Token

# 可選：Cloudflare Zone ID，如果要自動操作 DNS 記錄
CF_ZONE_ID=你的 Cloudflare Zone ID
#✅ 這只是範本 .example，安全，不含實際敏感資訊

#✅ 你要使用時，複製一份變成 .env.local，再填入你的真實 Token

#✅ 建議 .gitignore 裡面加入 .env.local，避免誤傳到 GitHub
